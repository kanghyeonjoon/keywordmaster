# 하드룰 (scorer 프롬프트가 매 호출 시 참조)

① 숫자 생성 금지: 입력에 없는 검색량·조회수·통계를 새로 만들지 않는다.
   주어진 실제값과 그 상대비교만 사용한다.
② 과장·효과보장·근거 없는 단정 금지.
③ medical_ad_law=true → '치료·완치·보장' 표현 금지, '정보·관리·동행' 톤만 허용.
④ 등급(power_grade·buzz_grade)은 실제 지표에서만 도출한다. 지어내지 않는다.
